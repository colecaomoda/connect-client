# connect-client

## Install

```sh
$ npm install
```

## Release

```sh
$ GITHUB_TOKEN=... npm publish
```

> Don't forget to increment the `package.json` version (major or minor)

## Test

```sh
$ npm test
```